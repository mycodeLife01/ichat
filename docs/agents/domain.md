# 领域文档

本项目采用 single-context 布局。

## 探索代码前

- 如根目录存在 `CONTEXT.md`，先读取其中的领域术语。
- 阅读 `docs/adr/` 中与当前工作相关的架构决策。
- 文件尚不存在时直接继续，不要求提前创建；待术语或决策实际形成时再由领域建模流程补充。

## 使用项目术语

Ticket、测试和设计文档应优先使用 `CONTEXT.md` 已定义的领域词汇，避免随意引入同义词。

## ADR 冲突

如果拟议工作与已有 ADR 冲突，必须明确指出冲突及重新讨论该决策的理由，不得静默覆盖。
