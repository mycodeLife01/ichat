# 2026-05-16 MVP Infra 交接

## 本次完成

- 将日志依赖从 `structlog` 切换为 `loguru`。
- 创建 `app/core/config.py`，统一读取 `.env` 和环境变量；必需配置缺失时启动失败。
- 创建 `app/core/logging.py`，配置 Loguru JSON 日志输出，并支持 `request_id` 上下文。
- 创建 `app/core/errors.py`，使用 `AppError` 表达 HTTP status code 和英文 `detail`。
- 创建 `app/main.py`，提供 FastAPI app factory、默认 `app`、request id middleware、异常处理、`/healthz` 和 `/readyz`。
- 创建 `app/db/base.py` 和 `app/db/session.py`，提供 SQLAlchemy async engine/session factory、`get_session` 和数据库 readiness helper。
- 初始化 Alembic 环境：`alembic.ini`、`alembic/env.py` 和 `alembic/versions/`。
- 更新 `compose.yml`，让 `api` 默认启动 `uvicorn app.main:app`，显式声明 `env_file: .env`，并只对 Compose 自身需要插值的端口变量使用 `${VAR:?message}`。
- 增加 core、api、db 基础设施测试。

## 本次刻意未做

- 未创建业务 ORM models。
- 未创建业务数据库迁移脚本。
- 未实现 auth、conversation、run、provider、context 或 worker 业务逻辑。
- 未调用真实 DeepSeek API。
- 未加入 OpenTelemetry、Prometheus 或额外日志格式开关。

## 验证命令

```bash
uv run pytest
uv run ruff check .
uv run mypy .
docker compose config
```

## 接下来的开发任务

建议按以下顺序继续：

1. 在 `app/models` 建立 users、refresh_tokens、email_verification_tokens 的 ORM models 和首个 Alembic 迁移。
2. 在 `app/schemas` 建立 auth 请求/响应 schemas。
3. 在 `app/services/auth` 实现 password hashing、JWT access token、refresh token service。
4. 在 `app/api` 增加认证路由，并复用当前 `AppError` 和 `get_session`。
5. 继续 conversation、message、run 和 provider 相关模型与服务。

## 注意事项

- 文档必须使用中文。
- 代码注释和 docstring 使用英文。
- 用户可见错误信息和应用提示使用英文。
- MVP 错误响应统一为 `{"detail": "English message"}`，错误类型由 HTTP status code 表示。
- 运行服务前必须准备 `.env` 或等价环境变量；缺少应用配置时应用启动失败，缺少 Compose 端口插值配置时 Docker Compose 失败。
- 当前 `worker` 服务仍是占位命令，不代表 worker loop 已可用。
