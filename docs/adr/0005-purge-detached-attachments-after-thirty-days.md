# 脱离当前消息的附件三十天后回收

消息编辑产生新修订后，只有当前消息修订的引用保护附件物理对象；曾被使用但已不再被任何当前修订引用的附件进入三十天回收期，到期后删除原件和派生物并释放配额。归档消息保留文件名、大小和摘要等元数据，Run transcript 继续自包含地保存模型当时实际读取的完整文档块，因此回收不会改写历史模型事实；该决定以放弃归档修订的永久原件下载能力，避免用户移除附件后仍无限期占用配额且只能通过删除整个会话才能释放。
