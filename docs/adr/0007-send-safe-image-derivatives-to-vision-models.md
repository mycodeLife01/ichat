# 视觉模型只接收安全图像派生物

iChat 向视觉模型发送经过完整解码、重新编码并去除元数据的不可变安全派生物，动画只发送首帧，附件原件永不进入 LLM provider；本期复用现有 `preview`，只有 provider 输入契约与预览契约实际分化时才新增专用派生物。`ImageBlock` 固化安全派生物快照而不固化存储位置或临时 URL，调用前快照校验失败则整次模型调用显式失败，不降级为附件提示或纯文字。该决定可能损失少量编码保真度，却把原件元数据和读取权限隔离在模型边界之外。
